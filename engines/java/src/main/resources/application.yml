spring:
  application:
    name: stratos-engines
  datasource:
    url: jdbc:postgresql://localhost:5432/stratos
    username: stratos
    password: changeme
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8002
